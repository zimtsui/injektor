{
  "name": "injektor",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "test": "tsc -b ./test/src/tsconfig.json && ava",
    "build": "tsc -b ./src/tsconfig.json",
    "clean": "rm -rf ./build"
  },
  "author": "Zim",
  "dependencies": {
    "@types/assert": "^1.5.6",
    "assert": "^1.5.0"
  },
  "devDependencies": {
    "ava": "^4.1.0"
  }
}
